apiVersion: v1
kind: Pod
metadata:
  name: demo-duct-tape
  labels:
    app: demo-duct-tape
spec:
  containers:
  - name: helloworld1
    image: busybox
    command: ['sh', '-c', 'echo "The app is running"; ls / /dbg; sleep 3600']
  - name: helloworld2
    image: busybox
    command: ['sh', '-c', 'echo "The app is running"; ls / /dbg; sleep 3600']
